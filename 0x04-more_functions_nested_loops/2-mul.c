#include "main.h"

/**
  * mul - multiply to integers
  * @a: mul parameters to multiply
  * @b: mul parameterto multiply
  * Return: product
  **/

int mul(int a, int b)
{
	int product;

	product = (a * b);
	return (product);
}
